// Placeholder. The API service is implemented in milestone M2 of the
// backend-split plan. See _presentation/whats-next.html and the M-plan in
// the chat transcript for context.
//
// Until M2 lands, running `npm run dev:api` is intentionally a no-op so that
// `npm run dev` (web) is unaffected by an empty workspace.
console.log("[espace-devhub/api] not implemented yet — see plan §5 milestone M2");
